[["pi package](https://github.com/mariozechner/pi) called `pi-bash-confirm` that intercepts bash tool calls and requires user confirmation before execution. It includes a Telegram notification system for blocked and modified commands.\n\n## Local Development\n\n### Installation\n\npi does not support installing packages from local directories. Use one of these methods:\n\n**Method 1: Copy extension file (recommended for development)**\n```bash\n# Global installation (applies to all projects)\ncp extensions/bash-confirm.ts ~/.pi/agent/extensions/\n\n# Project installation (current project only)\nmkdir -p .pi/extensions\ncp extensions/bash-confirm.ts .pi/extensions/\n```\n\n**Method 2: Add to settings.json**\n```bash\n# Global settings (~/.pi/agent/settings.json) or project settings (.pi/settings.json)\n{\n  \"packages\": [\"/absolute/path/to/pi-permission/extensions/bash-confirm.ts"], {"block": true, "choice": "Allow \u2192 return undefined (execute)\n  - Block \u2192 send notification", "mariozechner/pi-tui`": "Container` - Main container for overlay\n- `Box` - Styled background box for command display\n- `Text` - Text rendering with theme colors\n- `SelectList` - Interactive option selection with keyboard navigation\n\nThe confirmation dialog is rendered as an overlay centered on screen (width: 60", "minHeight": 10, "dependencies)": "telegramCall<T>()` - Generic HTTP request wrapper with timeout and IP family control\n- `buildBlockedMessage()` - Formats HTML notification for blocked commands\n- `buildModifiedMessage()` - Formats HTML notification for edited commands\n- `escapeHtml()` - Escapes HTML entities for Telegram's HTML parse mode\n- `truncateText()` - Truncates to 4096 char limit with smart break points\n\nNotifications are **fire-and-forget** - failures don't block the confirmation flow.\n\n### Settings Access Pattern\n\n```typescript\n// Load merged settings\nconst { settings", "bashConfirm.notifications.enabled": false, "means": "Type errors will only appear at runtime via pi\n- No package.json scripts for building\n- Extension file must be valid TypeScript that pi can load\n\n### Blocking in Non-Interactive Mode\n\nWhen `ctx.hasUI` is false (print mode", "patterns": "typescript\ntry {\n  if (new RegExp(pattern).test(command)) {\n    // Handle match"}, {"decisions": "Blocked: sent when user blocks or pattern matches\n- Modified: sent when user edits a command (original vs modified)\n\nThe `/bash-confirm test-notify` command sends a blocked notification for a test command.\n\n## Configuration Structure\n\n```json\n{", "bashConfirm": {"enabled": true, "safeCommands": ["^ls", "^git (status|log)"], "blockedCommands": ["rm -rf", "sudo .* rm"], "notifications": {"enabled": true, "onBlocked": true, "onModified": true, "onAllowed": false, "telegram": {"enabled": true, "token": "YOUR_BOT_TOKEN", "chatId": "YOUR_CHAT_ID", "timeoutMs": 5000, "forceIpv4": true}}}}, ["Type]Message()` function following the pattern of `buildBlockedMessage()`\n2. Create a `send[Type]Notification()` async function that calls `telegramCall()`\n3. Call the send function at the appropriate point in the event flow\n4. Add configuration options to settings schema if needed\n\n## File Structure\n\n```\npi-permission/\n\u251c\u2500\u2500 extensions/\n\u2502   \u2514\u2500\u2500 bash-confirm.ts       # Main extension (all code here)\n\u251c\u2500\u2500 docs/\n\u2502   \u251c\u2500\u2500 example-settings.json # Example configuration\n\u2502   \u251c\u2500\u2500 notifications.md      # Notification system documentation\n\u2502   \u2514\u2500\u2500 telegram-setup.md     # Telegram bot setup guide\n\u251c\u2500\u2500 examples/\n\u2502   \u251c\u2500\u2500 test-extension.ts     # Testing utilities and demo commands\n\u2502   \u2514\u2500\u2500 telegram-bot-creation.md # Quick start for bot creation\n\u251c\u2500\u2500 package.json              # npm package definition\n\u251c\u2500\u2500 README.md                 # User documentation\n\u251c\u2500\u2500 PLAN.md                   # Implementation plan (reference only)\n\u2514\u2500\u2500 AGENTS.md                 # This file\n```\n\n## Dependencies\n\n**Runtime:**\n- `@mariozechner/pi-coding-agent` - Extension API (peer dependency)\n- `@mariozechner/pi-tui` - UI components (peer dependency)\n- Node.js built-ins: `https`, `fs`, `path`, `os`\n\n**No external npm packages required** - uses only pi SDK and Node.js built-ins."]]