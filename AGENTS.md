# Agent Notes

- Target the `@earendil-works` Pi packages at `0.80.x`.
- Pi 0.80 exposes model completion helpers to extensions through `@earendil-works/pi-ai/compat`; its `ModelRegistry` does not expose the newer `Models` completion API.
- `PLAN.md` was an obsolete implementation plan and was intentionally removed. Do not recreate it as project documentation.
